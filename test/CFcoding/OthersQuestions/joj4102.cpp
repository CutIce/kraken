//
// Created by hsh on 2020/7/14.
//

#include <iostream>
#include <queue>
#include <algorithm>
#include <vector>
using namespace std;

int main(){
    int k,n,res=0;
    cin>>k>>n;
    vector<int> data(n);
    queue<int> q;
    for (int i=0;i<n;++i)
        cin>>data[i];
        if ( q.empty() ) {q.push(i);
            continue;}
    }

}